const E = module.exports;

E.logMessage = async (text) => {
    // Implement message debug logging as needed: log message to the console, send to a remote API, etc.
    return true;
};

E.logAudio = async (stream, text) => {
    // Implement audio debug logging as needed: store the audio stream locally, send to a remote API, etc.
    return true;
};
